20/04/2026

## react intro

    What is React?

    1) React is a JavaScript library (not a framework) developed by Facebook (Meta) for building user interfaces (UIs), especially Single Page Applications (SPAs).

    2) It helps developers build reusable UI components that update efficiently when data changes.

    Instead of reloading a whole webpage when something changes, React updates only the parts of the UI that need to change.

    -------
    Why do we need React?

	It makes UI development faster, cleaner, and more efficient.

	Provides a better user experience with smooth updates without page reloads.


    ## must need to install node.js
 
## procedure to initiate project

    step 1 : npm create vite@latest [project_name]
    step 2 : select react
    step 3 : select javascript+ react compailer
    step 4 : npm install                // install node module packages
    step 5 : npm run dev 

    if you want to re-run your project mean 
        npm run dev

21/04/2026

## Props

    Props is nothing but passing or drilling data from parent component to child component.

        Declearing a Props
        Destructuring a props

## conditional rendering

    conditional renderingmeans showing or hiding parts of UI based On some Condition .

            Ternary operator {condition?true:false}
            logical  operator && || !

22/04/2026

## list rendering 

    Rengering a multiple items dynamically using .map()


## style in react.js

    Golble style ------ app.css and  index.css is called as global style 
    external style----- to create a seperate css file for  component 
    Module  style ----- we need to create a file as eg: [filename.module.css]
    Inline style ------ we need calla style  atteibute in html tags with {{padding:"20px"}}
    Object  style ----- we need to declear a css  property as an object

27/04/2026

## useState() && [form &controlled component] 

    useState is a react hook that allows you to add state (data that can change over time)
    to a functional component.

    Normally , in js function ,variables disappear after  the function runs, but in useState
    ,react remember   values b/t re-render.

    js
    const num=3
    update 4
    value as 3

    react
    
    const num=3
    update 4  ,even funcction re-render


    syntax:

        const[state,setState]=useState(initialValue)

        eg : const[count,setCount]=useState(0)

        state--- tha current value  of  the state variables
        setState--- a function that update the state
        initialValue--- the starting value of the state

28/4/2026

## useEffect()

    In React ,useEffect is a hook that let you perform side effect in functionl component.
    Side effect are task that happen outside the normal react rendering process ,
        like  : 
                    fetching data  from  Applications
                    Updating the browser DOM manually
                    starting/ stoping timers

    syntax:
            useEffect(()=>{
                //code
            },[dependency 1, dependency 2.....])

            []--- dependency is empty means ,useEffect will  execute only once at begining.


## domain

    .com --- commercial
    .org --- Organization
    .net --- Network
    .edu --- Education
    .gov --- Government
    .mil --- Military
    .co  --- Company
    .info -- information
    .biz --- Business
    .in  --- india